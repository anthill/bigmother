{
  "name": "sensorver",
  "version": "0.0.1",
  "description": "A webserver to monitor a sensor",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.12.2",
    "odroid-servo": "0.0.1",
    "socket.io": "^1.3.5"
  }
}
